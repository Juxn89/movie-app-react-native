import { useLocalSearchParams } from 'expo-router'
import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const Saved = () => {
	return (
		<View>
			<Text>Movie Saved:</Text>
		</View>
	)
}

export default Saved

const styles = StyleSheet.create({})